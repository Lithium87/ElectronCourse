const POLLING_INTERVAL = 500;

export function pollResources() {
    setInterval(() => {
        // This function can be used to poll resources or perform periodic tasks.
        // Currently, it does nothing but can be extended as needed.
    }, POLLING_INTERVAL);
}